---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WritingFilters from '../../islands/WritingFilters';
import PostCard from '../../components/PostCard.astro';
import { getPosts } from '../../lib/posts';

const posts = await getPosts();
const counts = posts.reduce<Record<string, number>>((acc, p) => {
  acc[p.data.type] = (acc[p.data.type] ?? 0) + 1;
  return acc;
}, {});
---

<BaseLayout title="Writing">
  <main class="container writing-page">
    <div class="crumb"><span>$ </span>find ./writing -sort recent</div>
    <h1>Writing</h1>
    <p class="writing-lede">Long-form tutorials, short notes, and outbound links with commentary. Filter by type or tag below.</p>
    <WritingFilters client:load counts={counts} />
    <div class="rows">
      {posts.map((post) => (
        <div data-post-row data-type={post.data.type} data-hidden="false">
          <PostCard post={post} variant="row" />
        </div>
      ))}
      <div class="empty">// 0 results. try a different filter.</div>
    </div>
  </main>
</BaseLayout>
